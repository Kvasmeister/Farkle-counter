{
  "name": "kostky",
  "version": "1.0.0",
  "description": "Farkle počítadlo — zdroj v src/, nasazuje se složený index.html",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "kontrola": "node build.mjs --kontrola && node Testy/kontrola-modulu.mjs",
    "test": "node build.mjs --kontrola && node Testy/kontrola-modulu.mjs && node Testy/vse.mjs",
    "test:rychle": "node Testy/vse.mjs"
  },
  "dependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1"
  },
  "devDependencies": {
    "acorn": "^8.18.0",
    "acorn-walk": "^8.3.5",
    "esbuild": "^0.25.0"
  }
}
